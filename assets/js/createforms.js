import React from 'react';
import ReactDOM from 'react-dom';
import '../styles/createforms.scss';
import Formulaire from './components/createforms/Formulaire';

ReactDOM.render(
    <Formulaire />, document.getElementById('createforms'));
